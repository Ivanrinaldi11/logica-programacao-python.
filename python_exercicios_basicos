# Progama que lê um numero inteiro 
# e mostra seu antecessor e sucessor

n = int(input('Digite um numero inteiro: '))

antecessor = n - 1
sucessor = n + 1

print (f'O antecessor de {n} é {antecessor}')
print (f'O sucessor de {n} é {sucessor}')
